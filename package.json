{
  "name": "seeker",
  "version": "0.0.0",
  "description": "",
  "main": "dist/seeker.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "sass src/style.sass dist/style.css && coffee -o ./dist src/seeker.coffee",
    "watch": "coffee -w -o ./dist src/seeker.coffee",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "jquery": "~2.1.1"
  },
  "devDependencies": {
    "chai": "^1.9.1",
    "coffee-script": "^1.7.1",
    "mocha": "^1.21.4",
    "sass": "^0.5.0"
  }
}
